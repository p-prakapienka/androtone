# Androtone

A cross-platform JUCE 8 synthesizer plugin. The project is declared in `androtone.jucer`
(Projucer). The desktop build is driven by a hand-maintained CMake script that fetches JUCE via
CPM; the Android build uses Projucer's exported Gradle project.

## Targets

| Platform | Format     | Notes                                                                |
| -------- | ---------- | -------------------------------------------------------------------- |
| Desktop  | VST3       | Windows / macOS / Linux (via `Builds/desktop/CMakeLists.txt`)         |
| Desktop  | Standalone | `.exe` / `.app` / binary                                             |
| Android  | Standalone | APK, from the Projucer-exported Gradle project in `Builds/android/`  |

## Prerequisites

- CMake 3.22+
- A C++17 toolchain (MSVC / Clang / GCC)
- Network access on first configure (CPM downloads JUCE)

Android also needs:

- Android Studio + Android NDK
- JUCE 8.0.12 unpacked as `juce/` at the repository root

## Build (desktop)

```sh
cmake -S Builds/desktop -B Builds/desktop/cmake-build-release-visual-studio
cmake --build Builds/desktop/cmake-build-release-visual-studio --config Release
```

CLion users can just open `Builds/desktop/CMakeLists.txt`.

Artifacts:

- `Builds/desktop/cmake-build-release-visual-studio/Androtone_artefacts/Release/VST3/Androtone.vst3`
- `Builds/desktop/cmake-build-release-visual-studio/Androtone_artefacts/Release/Standalone/Androtone[.exe|.app]`

## Build (Android)

Re-save the project and build the release APK:

```sh
./juce/Projucer --resave androtone.jucer --lf
./Builds/android/gradlew -p Builds/android assembleRelease
```

The APK is written below `Builds/android/app/build/outputs/apk/`. The Gradle project,
`Builds/android/app/CMakeLists.txt`, and `JuceLibraryCode/` are generated by Projucer.

## Release artifacts

`.github/workflows/release-artifacts.yml` builds a Windows x86_64 ZIP and a universal Android APK
for pushes to `master`, tags, and manual runs. A successful push to `master` replaces the rolling
`nightly` prerelease with both files. Pushing a version tag such as `0.0.6`
publishes a normal, permanent GitHub Release for that tag. The APK is release-optimised but uses
an ephemeral CI debug key, so it is suitable for sideloaded testing rather than Play Store
distribution or in-place upgrades.

## Repo layout

```
.
├── androtone.jucer             # Projucer project
├── CLAUDE.md
├── README.md
├── resources/                  # checked-in embedded binary resources
├── docs/                       # requirements & design notes
├── scripts/                    # helper scripts (e.g. build-projucer.ps1)
├── source/                     # plugin C++
│   ├── PluginEditor.{h,cpp}
│   ├── PluginProcessor.{h,cpp}
│   ├── Sequencer/              # step sequencer
│   └── Engine/                 # audio engine
│       ├── SoundSources/       # voices and sounds
│       └── Processors/         # audio fx (consume + produce audio)
└── Builds/
    ├── desktop/CMakeLists.txt  # hand-maintained desktop build
    └── android/                # Projucer-exported Gradle project
```

## Notes on JUCE version

`Builds/desktop/CMakeLists.txt` pins JUCE 8.0.12 via CPM. If that tag is not available on
`juce-framework/JUCE`, bump `GIT_TAG` / `VERSION` to the current 8.0.x patch.

## Plugin identity

| Field                    | Value             |
| ------------------------ | ----------------- |
| Product name             | Androtone         |
| Company                  | Restricted Space  |
| Plugin code              | Antn              |
| Manufacturer code        | RstS              |
| Bundle ID                | com.restrictedspace.androtone |
